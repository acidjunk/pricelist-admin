const API_URL = process.env.NODE_ENV === "development" ? "http://127.0.0.1:8080" : "https://api.prijslijst.info";

export const imageUrl = "https://d3sticxdmgvhkp.cloudfront.net/";

export default API_URL;
